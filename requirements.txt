requests>=2.31
openpyxl>=3.1
python-dotenv>=1.0
# Note: playwright is only needed for the optional LOCAL email-gap scraper
# (legacy 2_enrich_emails.py --scrape-missing). The cloud jobs don't use it.
